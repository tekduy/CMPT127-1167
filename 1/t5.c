#include <stdio.h>
#include <math.h>

int main( void )
{
  int a = 0;
  float i = 0;
  int b = 0;

  int result = scanf( "%f", &i );
while (a < i > a)
{
if ( a < i > a )
{
  int x = floor(i);
  int y = round(i);
  int z = ceil(i);

  printf("%d %d %d\n", x, y, z );

  int result = scanf( "%f", &i );
}
if (i == 0)
{
  int b = i;
  int result = printf("scanf error: (%d)\n", b);
}
if ( result == EOF )
{
  printf("Done.\n");
}
}
}
